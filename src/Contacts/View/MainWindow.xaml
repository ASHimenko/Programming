<Window x:Class="View.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:View"
        mc:Ignorable="d"
        Title="MainWindow" MinWidth="500" MinHeight="400" Height="338" Width="489">
    
    <Grid Margin="15,15,15,15">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <GroupBox Header="Edit Contact" Grid.Row="0" Grid.Column="0" Margin="3">
            <StackPanel>
                <Label Content="Name:" />
                <TextBox x:Name="NameButton" Margin="3,3,3,3" />
                <Label Content="Phone Number:" />
                <TextBox x:Name="PhoneNumberButton" Margin="3,3,3,3" />
                <Label Content="Email:" />
                <TextBox x:Name="EmailButton" Margin="3,3,3,3" />
            </StackPanel>
        </GroupBox>

        <GroupBox Header="Read Contact" Grid.Row="0" Grid.Column="1" Margin="3">
            <StackPanel>
            </StackPanel>
        </GroupBox>

        <StackPanel Grid.Row="1" Grid.Column="1" 
                Orientation="Horizontal" 
                HorizontalAlignment="Right" 
                Margin="0,10,0,0">
            <Button Content="Save" Width="75" Height="25" Margin="3"/>
            <Button Content="Load" Width="75" Height="25" Margin="3"/>
        </StackPanel>

    </Grid>

</Window>
